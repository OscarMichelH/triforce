<h1>Shopping Cart</h1>
<hr>
<p id="notice"><%= notice&.html_safe %></p>


<% @cart.books.each do |book| %>
  <div class="row">
  <div class="col-2">
      <div class="card" style="width: 18rem;">
        <div class="card-body">
          <h5 class="card-title"><%= book.title %>
            <%= link_to delete_book_path(book), method: :delete, data: { confirm: '¿Are you sure?' } do %>
              <i class="bi bi-trash3-fill text-danger"></i>
            <% end %>
          </h5>
          <h6 class="card-subtitle mb-2 text-muted">Author: <%= book.author %></h6>
          <p class="card-text"><%=book.description&.truncate(25) %></p>
          <a href="#" class="card-link">Price: <%= book.price %></a>
        </div>
      </div>
    <br>
    </div>

    <!--    <tr>-->
    <!--      <td><%#= link_to 'Show', book %></td>-->
    <!--      <td><%#= link_to 'Edit', edit_book_path(book) %></td>-->
    <!--      <td><%#= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
    <!--    </tr>-->
  </div>
<% end %>
<div class="row">
  <div class="col-12">
    <p class="d-inline mr-4">Total: <%= @cart.books&.sum(:price) %></p>
    <%= link_to 'Buy With Balance', pay_cart_path, class: 'btn btn-primary d-inline' %>

  </div>
</div>